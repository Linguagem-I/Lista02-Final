#include <stdio.h>
#include <stdlib.h>
#include <string.h>
lerPares();

int main {
  lerPares();

  return 0;
}

int lerPares(){
  int i, x , y, quociente, resto;
  for (i=0;i<5;i++){
    scanf ("%d %d", &x, &y)
    quociente = x / y;
    printf("O quociente no par %d: %d \n", i+1, quociente);
    resto = x%y;
    printf("O resto no par %d : %d \n", i+1, resto);
  }
}
